# Dockerize-a-React-application
# 🚀 Dockerize React App with Multi-Stage Build

This project demonstrates how to **Dockerize a React application** using a **multi-stage Docker build**.  
Multi-stage builds optimize the image size by separating the build stage (Node.js) and runtime stage (Nginx).

---

## 🧩 Project Overview
This setup allows you to:
- Build and bundle a React app using Node.js
- Serve the production-ready files using Nginx
- Keep your Docker image lightweight and fast

---

## 🏗️ Folder Structure
